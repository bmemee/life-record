{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter config --no-analytics && ./flutter/bin/flutter pub get && ./flutter/bin/dart run build_runner build --delete-conflicting-outputs && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "echo 'skip'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}